# 画像挿入・ポップアップ機能 再現プロンプト

以下のプロンプトを使用して、全く同じ画像挿入機能とポップアップ表示機能を他のシステムに実装してください。

## 📋 実装要件

### 1. 基本機能
- **画像アップロード機能**: ファイル選択による画像挿入
- **画像クロップ機能**: 円形クロップ（ドラッグ移動・ズーム調整）
- **画像表示機能**: テーブル内での48x48px円形アバター表示
- **ポップアップ表示機能**: アバタークリックで300x300px拡大表示

### 2. 技術仕様
- **フレームワーク**: React + Next.js + TypeScript
- **UIライブラリ**: Material-UI (MUI)
- **画像処理**: Canvas API（クライアントサイド）
- **データ形式**: Base64エンコード文字列

## 🔧 詳細実装プロンプト

```
React + Next.js + TypeScript + Material-UIを使用して、以下の画像挿入・表示機能を実装してください：

### 【画像アップロード機能】
1. **ファイル入力**
   - `accept="image/jpeg,image/jpg,image/png,image/gif,image/webp,image/bmp"`
   - 最大ファイルサイズ: 5MB
   - エラーハンドリング: ファイル破損、サイズ超過、形式不正

2. **バリデーション処理**
   ```typescript
   const validateImage = (file: File): string | null => {
     if (file.size > 5 * 1024 * 1024) return 'ファイルサイズが5MBを超えています';
     const validTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/gif', 'image/webp', 'image/bmp'];
     if (!validTypes.includes(file.type)) return '対応していない画像形式です';
     return null;
   };
   ```

### 【画像クロップ機能】
1. **クロップダイアログ**
   - 300x300pxのプレビューエリア（円形マスク）
   - 画像のドラッグ移動機能
   - マウスホイール・2本指ジェスチャーでズーム
   - ズーム倍率表示（パーセント）
   - 操作説明テキスト

2. **ドラッグ・ズーム実装**
   ```typescript
   const [crop, setCrop] = useState({ x: 0, y: 0, width: 200, height: 200 });
   const [zoom, setZoom] = useState(1);
   
   // マウスドラッグ
   const handleMouseDown = (e: React.MouseEvent) => {
     const startX = e.clientX - crop.x;
     const startY = e.clientY - crop.y;
     
     const handleMouseMove = (e: MouseEvent) => {
       setCrop(prev => ({
         ...prev,
         x: e.clientX - startX,
         y: e.clientY - startY
       }));
     };
     
     document.addEventListener('mousemove', handleMouseMove);
     document.addEventListener('mouseup', () => {
       document.removeEventListener('mousemove', handleMouseMove);
     }, { once: true });
   };
   
   // ズーム機能
   const handleWheel = (e: React.WheelEvent) => {
     e.preventDefault();
     setZoom(prev => Math.max(0.5, Math.min(3, prev + (e.deltaY > 0 ? -0.1 : 0.1))));
   };
   ```

3. **Canvas処理（円形クロップ）**
   ```typescript
   const applyCrop = useCallback(() => {
     const canvas = canvasRef.current;
     const image = imageRef.current;
     if (!canvas || !image) return;
     
     const ctx = canvas.getContext('2d');
     const size = 300;
     canvas.width = size;
     canvas.height = size;
     
     // 円形クリッピング
     ctx.beginPath();
     ctx.arc(size/2, size/2, size/2, 0, Math.PI * 2);
     ctx.clip();
     
     // 画像描画
     ctx.drawImage(
       image,
       crop.x * zoom, crop.y * zoom,
       size / zoom, size / zoom,
       0, 0, size, size
     );
     
     // Base64変換
     const base64 = canvas.toDataURL('image/jpeg', 0.9);
     setProfileImage(base64);
   }, [crop, zoom]);
   ```

### 【テーブル表示機能】
1. **アバター表示**
   ```jsx
   <Avatar
     src={staff.profileImage}
     sx={{ 
       width: 48, 
       height: 48,
       cursor: staff.profileImage ? 'pointer' : 'default',
       '&:hover': staff.profileImage ? {
         opacity: 0.8,
         transform: 'scale(1.05)',
         transition: 'all 0.2s ease-in-out'
       } : {}
     }}
     onClick={() => staff.profileImage && handleImageModalOpen(staff.profileImage)}
   >
     {!staff.profileImage && <Person />}
   </Avatar>
   ```

### 【ポップアップ表示機能】
1. **画像表示モーダル**
   ```jsx
   <Dialog
     open={imageModalOpen}
     onClose={handleImageModalClose}
     maxWidth={false}
     PaperProps={{
       sx: {
         backgroundColor: 'transparent',
         boxShadow: 'none',
         overflow: 'visible'
       }
     }}
     BackdropProps={{
       sx: {
         backgroundColor: 'rgba(0, 0, 0, 0.5)'
       }
     }}
   >
     <Box sx={{ 
       position: 'relative',
       display: 'flex',
       justifyContent: 'center',
       alignItems: 'center',
       p: 0
     }}>
       <Avatar
         src={selectedImage}
         sx={{ 
           width: 300, 
           height: 300,
           border: '3px solid white',
           boxShadow: '0 4px 20px rgba(0, 0, 0, 0.3)'
         }}
       />
       <IconButton
         onClick={handleImageModalClose}
         sx={{
           position: 'absolute',
           top: -15,
           right: -15,
           backgroundColor: 'rgba(255, 255, 255, 0.95)',
           border: '2px solid #ddd',
           width: 32,
           height: 32,
           '&:hover': {
             backgroundColor: 'rgba(255, 255, 255, 1)',
             transform: 'scale(1.1)',
             transition: 'all 0.2s ease-in-out'
           }
         }}
       >
         <Close fontSize="small" />
       </IconButton>
     </Box>
   </Dialog>
   ```

### 【状態管理】
```typescript
// 画像クロップ関連の状態
const [cropDialogOpen, setCropDialogOpen] = useState(false);
const [originalImage, setOriginalImage] = useState<string>('');
const [crop, setCrop] = useState({ x: 0, y: 0, width: 200, height: 200 });
const [zoom, setZoom] = useState(1);

// 画像表示モーダル状態
const [imageModalOpen, setImageModalOpen] = useState(false);
const [selectedImage, setSelectedImage] = useState<string>('');

// Refs
const canvasRef = useRef<HTMLCanvasElement>(null);
const imageRef = useRef<HTMLImageElement>(null);
```

### 【必要なインポート】
```typescript
import React, { useState, useRef, useCallback } from 'react';
import {
  Dialog, DialogContent, DialogActions, Button, Box, Avatar, 
  IconButton, Typography
} from '@mui/material';
import { Person, Close } from '@mui/icons-material';
```

### 【データ型定義】
```typescript
interface DataMember {
  id: string;
  name: string;
  profileImage?: string; // Base64エンコードされた画像データ
  // その他のフィールド...
}
```

### 【重要な実装ポイント】
1. **画像のBase64変換**: FileReaderとCanvas APIを使用
2. **円形クロップ**: Canvas の `arc()` と `clip()` を使用
3. **ドラッグ操作**: マウスイベントで座標計算
4. **ズーム機能**: WheelEventとTouchEventに対応
5. **エラーハンドリング**: ファイル読み込み失敗時の処理
6. **レスポンシブ対応**: 画面サイズに応じた表示調整
7. **アクセシビリティ**: キーボード操作とARIAラベル

### 【スタイリングの詳細】
- **アバターホバー**: `transform: scale(1.05)` + `opacity: 0.8`
- **モーダル背景**: 透明 + 薄いグレーオーバーレイ
- **閉じるボタン**: 白背景 + グレー境界線 + ホバー拡大
- **画像シャドウ**: `box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3)`
- **トランジション**: `transition: all 0.2s ease-in-out`

この仕様通りに実装すれば、全く同じ機能を再現できます。
```

## 📁 ファイル構成例

```
components/
├── ImageUpload/
│   ├── ImageCropDialog.tsx    # クロップダイアログ
│   ├── ImageModal.tsx         # 画像表示モーダル
│   └── AvatarWithPreview.tsx  # クリック可能なアバター
├── Table/
│   └── DataTable.tsx          # メインテーブル
└── types/
    └── common.ts              # 型定義
```

## 🎯 実装チェックリスト

- [ ] ファイル選択機能（5MB制限、形式チェック）
- [ ] 画像プレビュー表示
- [ ] ドラッグ移動機能
- [ ] ズーム機能（マウス・タッチ対応）
- [ ] 円形クロップ処理
- [ ] Base64変換・保存
- [ ] テーブル内アバター表示（48x48px）
- [ ] ホバーエフェクト
- [ ] クリック可能性の制御
- [ ] ポップアップモーダル（300x300px）
- [ ] 透明背景 + オーバーレイ
- [ ] 閉じるボタン
- [ ] エラーハンドリング
- [ ] レスポンシブ対応

このプロンプトを使用することで、全く同じ機能を他のシステムに実装できます。

